# Pytorch Implementation of Truncated Graph Transformers. 

# Usage
Please create a conda environment with the yml file (You may need to adjust the cuda version for pytorch and dgl) and run
python main.py
